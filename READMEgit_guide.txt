VS Code:
Open correct folder in vs
Open new git bash terminal

Make sure that you're in the group_assignment_1 repository

!Note: before adding any new change 
git fetch --all
git pull --all #in case new changes were displayed

!Note changes in the code should be first pushed to [data_raw, model_raw, sandbox]
Before doing any change make sure you are under the right branch 
to move between branches 
git checkout (Name of the branch you want to add the change at)


Commit your changes
git add . #this would add all your changes for your commit
git commit -m "add your commit message here"
Finally in vs code select synchronize 
